const Tasky = function () {
  this.fps = 60;
  this.setFPS = function (fps) {
    if (fps == this.fps) {
      console.log("FPS was already set to " + fps);
    } else {
      this.fps = fps;
    }
  };
};
